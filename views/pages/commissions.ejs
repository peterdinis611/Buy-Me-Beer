<%- include('../partials/head', { title }) %>
<%- include('../partials/dashboard-shell-open', { active: 'commissions', user }) %>

<div class="mx-auto max-w-4xl">
  <header class="page-header">
    <h1 class="page-title">Commissions</h1>
    <p class="page-subtitle">Manage client requests and payment status.</p>
  </header>

  <% if (items.length === 0) { %>
    <div class="card-static py-12 text-center text-muted">No commission requests yet.</div>
  <% } else { %>
    <div class="space-y-4">
      <% items.forEach(function(item) { %>
        <div class="card-static text-left">
          <div class="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between">
            <div>
              <p class="font-semibold"><%= item.title %></p>
              <p class="mt-1 text-sm text-muted"><%= item.clientName %><% if (item.clientEmail) { %> · <%= item.clientEmail %><% } %></p>
              <% if (item.description) { %><p class="mt-3 text-sm text-app-text"><%= item.description %></p><% } %>
              <p class="mt-3 text-lg font-bold text-brand-beer"><%= formatMoney(item.price) %></p>
            </div>
            <div class="flex flex-col gap-2 sm:items-end">
              <span class="rounded-full bg-white/10 px-3 py-1 text-xs uppercase tracking-wide"><%= item.status.replace('_', ' ') %></span>
              <% if (item.status === 'pending') { %>
                <a href="/commission/<%= item.id %>/pay" class="btn-primary text-sm text-center">Client payment link</a>
              <% } %>
              <form action="/dashboard/commissions/<%= item.id %>/status" method="post" class="flex flex-wrap gap-2">
                <select class="input py-2 text-sm" name="status">
                  <option value="pending" <%= item.status === 'pending' ? 'selected' : '' %>>Pending</option>
                  <option value="paid" <%= item.status === 'paid' ? 'selected' : '' %>>Paid</option>
                  <option value="in_progress" <%= item.status === 'in_progress' ? 'selected' : '' %>>In progress</option>
                  <option value="done" <%= item.status === 'done' ? 'selected' : '' %>>Done</option>
                  <option value="canceled" <%= item.status === 'canceled' ? 'selected' : '' %>>Canceled</option>
                </select>
                <button type="submit" class="btn-secondary text-sm">Update</button>
              </form>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  <% } %>
</div>

<%- include('../partials/dashboard-shell-close') %>
<%- include('../partials/dashboard-footer') %>
