<%- if (typeof flash !== 'undefined' && flash) { %>
<div
  x-data="flashMessage()"
  x-show="visible"
  x-transition:enter="transition ease-out duration-400"
  x-transition:enter-start="opacity-0 -translate-y-3 scale-95"
  x-transition:enter-end="opacity-100 translate-y-0 scale-100"
  x-transition:leave="transition ease-in duration-300"
  x-transition:leave-start="opacity-100 translate-y-0 scale-100"
  x-transition:leave-end="opacity-0 -translate-y-2 scale-95"
  class="mx-auto w-full max-w-6xl px-4 pt-4"
>
  <div class="<%= flash.type === 'success' ? 'flash-success' : flash.type === 'error' ? 'flash-error' : 'flash-info' %> flex items-start justify-between gap-3">
    <span><%= flash.message %></span>
    <button type="button" @click="visible = false" class="shrink-0 text-white/50 hover:text-white" aria-label="Dismiss">×</button>
  </div>
</div>
<% } %>
